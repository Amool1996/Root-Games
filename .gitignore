# downloaded cluster bundles (already extracted into cluster/*_results/)
*.tar.gz

# local tooling / scratch
.claude/
Thumbs.db
.DS_Store

# voice files the installer replaced, kept locally so a bad take can be undone
ra-data/voice/_replaced/
