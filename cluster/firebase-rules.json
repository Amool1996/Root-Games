{
  "rules": {
    "scores": {
      "$day": {
        ".read": true,
        "$id": {
          ".write": "!data.exists()",
          ".validate": "newData.hasChildren(['n','s','e','g']) && newData.child('n').isString() && newData.child('n').val().length <= 14 && newData.child('s').isNumber() && newData.child('s').val() >= 0 && newData.child('s').val() <= 100 && newData.child('e').isString() && newData.child('g').isNumber()"
        }
      }
    }
  }
}
